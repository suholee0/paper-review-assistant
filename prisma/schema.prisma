generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:../data/papers.db"
}

model Paper {
  id            String      @id @default(uuid())
  title         String
  authors       String?
  publishedDate String?
  url           String?
  filePath      String
  chatSessionId String?
  createdAt     DateTime    @default(now())
  highlights    Highlight[]
}

model Highlight {
  id          String   @id @default(uuid())
  paperId     String
  paper       Paper    @relation(fields: [paperId], references: [id], onDelete: Cascade)
  page        Int
  rects       String   // JSON: [{top, left, width, height}] as percentages relative to page
  text        String   @default("")  // The highlighted text
  color       String   @default("yellow")
  memo        String?
  createdAt   DateTime @default(now())
}
